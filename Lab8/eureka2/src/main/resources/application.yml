server:
  port: 8762
eureka:
  instance:
    hostname: eurekaserver2
    appname: eureka-cluster
  environment: local
  client:
    serviceUrl:
      defaultZone:
        http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true